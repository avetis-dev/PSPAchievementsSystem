# Security Policy

This project runs as a kernel plugin on a game console. Bugs may crash a game or require a full restart of the device.

Do not publicly post vulnerabilities that could cause arbitrary kernel memory writes or code execution. Report them privately to the repository owner through the contact method listed on the GitHub profile.

Never attach account credentials, tokens, private service responses, or unredacted personal logs to public issues.
